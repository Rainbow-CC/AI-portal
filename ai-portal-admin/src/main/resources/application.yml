server:
  port: 8081

spring:
  application:
    name: ai-portal-admin
  datasource:
    url: jdbc:mysql://localhost:3306/ai_portal?useSSL=false&serverTimezone=UTC
    username: root
    password: root123456
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  config: classpath:logback-spring.xml
  level:
    root: INFO
    com.ai.portal.admin: DEBUG

dashscope:
  api-key: ${DASHSCOPE_API_KEY:}
