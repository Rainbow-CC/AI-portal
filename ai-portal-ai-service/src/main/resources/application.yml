server:
  port: 8082

spring:
  application:
    name: ai-portal-ai-service
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

dashscope:
  api-key: ${DASHSCOPE_API_KEY:}
